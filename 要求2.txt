生成csv（一般是spin格式的文章拿来生成）生成的csv必须是utf-8格式的 注意测试不能生成乱码
 title放在1.txt；文章放在存放一个1目录。（为了方便理解这么写，要求是要写成可以指定目录）
 从1.txt和1目录读取文章存放于CSV。CSV分为两栏，A栏放title，也就是1.txt中的1行；B栏放文章，也就是1目录下的文章。可以指定一次生成多少个csv（比如10个，20个），每个csv多少行（比如20行，25行），也就是放多少文章。生成csv的时候用过的文章txt不能重复使用，最好使用过的另存到其他目录。title的话按1.txt逐行（行数不够就循环）或者随机两种选择生成。